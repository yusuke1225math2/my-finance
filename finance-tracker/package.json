{
  "name": "finance-tracker-gas",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-appsscript.mjs",
    "push": "npm run build && clasp push",
    "pull": "clasp pull",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.8",
    "typescript": "^5.9.3"
  }
}
