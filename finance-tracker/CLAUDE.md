# finance-tracker

GAS（TypeScript）でVpass/楽天カードの通知処理などを行うサブプロジェクト。

## 編集後の必須手順

`src/` 配下を編集したら、確認なしで必ず以下を実行すること。
1. `npm run build` でTypeScriptのコンパイルが通ることを確認する
2. `npm run push` でビルド＋clasp pushまで完了させる

（毎回確認を求められるのが手間なので、編集が終わったら即build＆push）
