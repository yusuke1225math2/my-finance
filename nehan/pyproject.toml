[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nehan-discord-automation"
version = "0.1.0"
description = "Discord Finance Report Generator"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
